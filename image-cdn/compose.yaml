services:
  app:
    build:
      context: .
    init: true
    restart: unless-stopped
    ports:
      - "2283:2283"
    # env_file: ./.env  # Uncomment if .env file exists
    networks:
      - app_network
networks:
  app_network:
    driver: bridge